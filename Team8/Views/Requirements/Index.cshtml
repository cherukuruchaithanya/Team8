@model IEnumerable<Team8.Models.Requirement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RequirementAbbr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequirementName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsSummer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsSpring)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsFall)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RequirementAbbr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequirementName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSummer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSpring)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsFall)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequirementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RequirementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RequirementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
